<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="container">
    <br/>
    <%= f.text_area :content, class: "form-control", rows: "6", placeholder: "Write your thoughts here"%><br/>
      <div class="container" id="share"><%= f.submit "Share your post with us!", class: "btn btn-danger btn-lg btn-block", id: "ttt" %></div>
  </div>
 
 
<% end %>
